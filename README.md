# CANOE Transportation Backend

This repository contains the v2.0 refactor scaffold for the CANOE transportation backend.

The current implementation is an early configuration-control layer only. It loads YAML configuration, validates required scaffold keys, creates configured working directories, and writes a setup smoke status artifact. It does not download sources, transform parameters, or compile SQLite databases yet.
